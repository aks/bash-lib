Things to do
============

* write more tests for date-utils: date_adjust, date_add, date_sub, days_between

* update README for new date-utils functions (tomorrow, yesterday, get_date_x_days ...)

* Create the check_stdout, check_stderr, etc. functions.

* write tests for run-utils.sh
  - add tests for add_trap

* write more tests for arg-utils.sh

* create a test suite for test-utils.sh: test-test-utils.sh :-)

* check for co-dependencies in all the -util.sh libraries.  Eg: mutually sourcing each other.
